import 'package:flutter/material.dart';

class SplitTheBillScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Split The Bill'),
      ),
      body: Center(
        child: Text('Split The Bill Page'),
      ),
    );
  }
}
